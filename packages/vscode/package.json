{
  "name": "doc-jit",
  "publisher": "jcalixte",
  "version": "1.1.5",
  "description": "A documentation just in time to access the right doc when you need it the most",
  "icon": "./assets/doc-jit.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jcalixte/doc-jit",
    "directory": "packages/vscode"
  },
  "categories": [
    "Education",
    "Visualization"
  ],
  "main": "./dist/index.js",
  "engines": {
    "vscode": "^1.76.0"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "scripts": {
    "build": "tsup src/index.ts --external vscode",
    "dev": "tsup src/index.ts --external vscode --sourcemap",
    "compile": "yarn run dev",
    "watch": "tsup src/index.ts --external vscode --sourcemap --watch",
    "vscode:prepublish": "yarn run build",
    "prepublishOnly": "yarn run build",
    "publish": "vsce publish --no-dependencies",
    "pack": "vsce package --no-dependencies"
  },
  "contributes": {
    "commands": [
      {
        "category": "doc-jit",
        "command": "docjit.open",
        "title": "Open the associated documentation",
        "icon": "$(book)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "docjit.open",
          "group": "navigation",
          "when": "editorTextFocus"
        }
      ]
    }
  },
  "dependencies": {
    "@cjs-exporter/globby": "^13.1.3",
    "minimatch": "^7.4.3"
  },
  "devDependencies": {
    "@types/node": "^18.15.10",
    "@types/vscode": "^1.76.0",
    "tsup": "^6.7.0"
  }
}